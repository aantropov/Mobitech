precision mediump float;

varying vec4 v_color;
varying vec2 v_texcoords;
uniform vec3 time;
uniform sampler2D texture;

void main() 
{
    vec4 color = texture2D(texture, v_texcoords);// + vec2(sin(time.y + v_texcoords.x), sin(time.y + v_texcoords.y));
    gl_FragColor = color * v_color;
}